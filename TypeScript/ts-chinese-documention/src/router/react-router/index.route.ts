import { NavigationType } from "src/types/navigation";

const ReactRouter: NavigationType = {
  name: "React Router",
  children: [
    // {
    //   name: "Get Started",
    //   children: GetStarted,
    // },
  ],
};

export default ReactRouter;
