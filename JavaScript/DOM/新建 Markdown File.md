1. chrome

   ```js
   var div=document.getElementById("id")
   window.getComputedStyle(div).fontSize
   ```

2. IE

   ```js
   var div = document.getElementById("id");
   div.currentStyle['font-size'];
   ```

   